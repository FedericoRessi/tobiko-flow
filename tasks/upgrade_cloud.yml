
---

- include_role:
    name:
      tobiko
  vars:
    tobiko_params:
      method: upgrade_cloud
      version: "{{ tobiko_upgrade_cloud_version | default(null) }}"
      fast: "{{ tobiko_upgrade_cloud_fast | default(false) | bool }}"
